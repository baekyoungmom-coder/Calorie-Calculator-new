# AGENTS.md

## 프로젝트 개요
Calorie Calculator는 사용자가 음식 사진 또는 텍스트 입력으로 식사 기록을 남기고, 칼로리를 추정하는 앱이다.

## 작업 원칙
- 사진 입력과 텍스트 입력을 모두 지원한다.
- 칼로리는 항상 추정치로 표시한다.
- 사용자가 직접 수정한 값을 최종값으로 반영한다.
- 민감한 정보는 불필요하게 저장하지 않는다.
- UI와 API는 각각 `UI_REFERENCE.md`, `API_CONTRACT.md`를 따른다.

## 구현 우선순위
1. 입력 방식 선택
2. 사진/텍스트 입력 화면
3. 칼로리 추정 결과 화면
4. 오늘 기록 및 마이페이지
5. 관리자 기능(필요 시)

## 완료 기준
- 로컬 빌드가 성공한다.
- 주요 화면 이동이 동작한다.
- 기록 저장이 가능하다.

## 환경변수와 보안

- API 키와 시크릿은 코드나 MD 파일에 직접 작성하지 않는다.
- 로컬 개발 값은 `.env.local`에 저장한다.
- `.env.local`의 실제 값을 읽어서 응답에 출력하지 않는다.
- `.env.local`은 GitHub에 Commit하지 않는다.
- 필요한 환경변수 이름만 `.env.example`에 기록한다.
- `NEXT_PUBLIC_`으로 시작하는 값은 브라우저에 노출된다는 점을 고려한다.
- service role key와 외부 API secret은 서버에서만 사용한다.
