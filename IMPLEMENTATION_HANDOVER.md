# 구현 현황 및 다음 작업 인수인계

최종 갱신: 2026-07-29

다음 작업을 시작할 때는 이 문서를 먼저 읽고, 세부 규칙은 `PROJECT_BRIEF.md`, `API_CONTRACT.md`, `DATA_MODEL.md`를 따른다.

## 현재 동작하는 범위

- 모바일 중심의 파스텔 UI가 구현되어 있다.
- 시작, 로그인, 입력 방식 선택, 사진 입력, 텍스트 입력, 결과, 오늘 기록, 지난 기록(마이페이지), 기록 수정 화면이 있다.
- 텍스트 입력을 바탕으로 한 간단한 로컬 칼로리 추정이 동작한다. 결과는 항상 추정치로 표시하며 사용자가 최종 칼로리를 수정할 수 있다.
- Google OAuth 로그인, 로그아웃, 로그인 세션 유지가 동작한다.
- 로그인한 사용자는 Supabase에 식사 기록을 생성·조회·수정·삭제할 수 있다.
- 오늘 기록은 기기 IANA 시간대를 전달하여 날짜별 합계를 계산한다.
- 사진 원본과 모델의 전체 응답은 현재 저장하지 않는다.

## 이미 적용한 외부 설정

### Supabase

- `profiles`, `meal_records`, `calorie_estimates` 테이블과 RLS 정책이 적용되어 있다.
- 적용 대상 마이그레이션:
  - `supabase/migrations/20260728210000_create_calorie_calculator_schema.sql`
  - `supabase/migrations/20260728213000_complete_existing_profiles_schema.sql`
- Google 로그인 공급자가 활성화되어 있고, 로그인 시 `profiles`를 동기화하는 트리거가 포함되어 있다.
- Auth URL 설정에는 운영 앱 주소의 `/auth/callback`과 로컬 개발 주소의 `/auth/callback`이 허용되어 있어야 한다.

### Vercel

- 운영 프로젝트: `calorie-calculator-new`
- 운영 앱: `https://calorie-calculator-new.vercel.app`
- GitHub `main` 브랜치의 최신 커밋을 운영 배포한다.
- 환경 변수는 Production과 Preview에 모두 설정한다.
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`
  - `NEXT_PUBLIC_APP_URL`
- 2026-07-29에 `NEXT_PUBLIC_APP_URL`을 운영 앱 주소로 수정하고 재배포했다. 이 값이 이전 Vercel 주소를 가리키면 OAuth 콜백이 다른 배포로 이동해 로그인 상태가 게스트로 보일 수 있다.

실제 키, URL 세부값, 비밀값은 문서나 Git에 기록하지 않는다. 로컬 값은 `.env.local`, 배포 값은 Vercel Environment Variables에서 관리한다.

## 확인한 결과

- 운영 배포가 Ready 상태인 것을 확인했다.
- 운영 앱에서 Google 로그인 후 홈 화면에 로그인한 사용자 정보가 표시되는 것을 확인했다.
- 이어서 `/mypage`에서 게스트 안내가 아닌 로그인 사용자 정보와 저장된 기록 화면이 표시되는 것을 확인했다.

## 아직 구현 또는 확정이 필요한 필수 사항

### 1. 핵심 기능 완성

1. 실제 칼로리 추정 서비스를 결정하고 연결한다.
   - 현재는 `src/lib/meals.ts`의 단순 규칙 기반 추정이다.
   - 사진 분석 또는 텍스트 기반 외부 모델을 사용할지 결정해야 한다.
   - 서버 Route에서만 비밀 API 키를 사용하고, 원본 모델 응답은 저장하지 않는다.
2. 사진 입력을 실제 분석 흐름으로 완성한다.
   - 파일 형식·용량·실패 처리 기준을 정한다.
   - 현재 정책대로 원본 사진은 분석 후 저장하지 않는 것을 기본값으로 유지한다.
3. 게스트 체험 정책을 구현한다.
   - 합의된 방향: 쿠키 기반으로 게스트 사용 3회까지 허용한다.
   - 쿠키 삭제·브라우저 변경으로 제한을 우회할 수 있다는 점은 테스트 단계에서 허용한다.
   - 3회 도달 시 로그인 안내와 저장 불가 처리를 명확히 표시한다.

### 2. 사용자 경험 및 안정성

1. 로그인하지 않은 상태에서 저장하려 할 때의 안내와 로그인 후 원래 화면 복귀를 점검한다.
2. 식사 기록 생성·수정·삭제를 실제 사용자 시나리오로 끝까지 테스트한다.
3. 빈 상태, 네트워크 오류, Supabase 오류, 중복 저장에 대한 안내를 보완한다.
4. 모바일 실제 기기에서 사진 선택, 시간대, 화면 스크롤을 검증한다.
5. 접근성 점검: 입력 라벨, 키보드 이동, 오류 메시지, 색 대비를 확인한다.

### 3. 출시 전 보안 및 운영

1. Supabase RLS를 다른 계정으로 재검증한다. 다른 사용자의 기록 ID를 직접 요청해도 조회·수정·삭제되지 않아야 한다.
2. Vercel Preview/Production 환경 변수에 누락이 없는지 확인한다. 값 변경 후에는 반드시 새 배포를 실행한다.
3. Supabase Auth의 Site URL과 Redirect URLs에 현재 운영 주소 및 로컬 개발 주소가 남아 있는지 점검한다.
4. `.env.local`, API 키, 서비스 역할 키가 Git에 포함되지 않았는지 점검한다.
5. 사용 중인 Vercel 프로젝트를 하나로 정리한 뒤, 초기 커밋만 가진 불필요 프로젝트는 운영 앱을 확인한 후 삭제한다.

## 권장 작업 순서

1. `TEST_CHECKLIST.md`의 로그인·저장·조회·수정·삭제 항목을 실제 운영 앱에서 점검한다.
2. 게스트 3회 제한을 구현하고, 로그인 전후 흐름을 보완한다.
3. 실제 칼로리 추정 방식과 사진 분석 정책을 결정한 뒤 API를 구현한다.
4. 오류 처리·모바일 점검·접근성 점검을 마친다.
5. 두 계정으로 RLS와 배포 환경 변수를 최종 검증한다.

## 다음 작업 시작 명령

```powershell
npm install
npm run dev
```

로컬에서는 `.env.local`에 필요한 환경 변수 이름을 설정한다. 값은 `ENVIRONMENT.md`를 참고하되, 실제 값은 채팅이나 문서에 붙여 넣지 않는다.

## 최근 관련 커밋

- `871a094` — OAuth 콜백 후 세션 쿠키 유지 수정
- `807f836` — Supabase 식사 기록 흐름 및 파스텔 UI 추가
