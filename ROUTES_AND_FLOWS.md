# ROUTES_AND_FLOWS.md

## 목적

화면 경로와 사용자 흐름을 정리한다.

## 라우트

- `/` : 시작 화면
- `/login` : 로그인 화면
- `/record` : 입력 방식 선택 화면
- `/record/photo` : 사진 입력 화면
- `/record/text` : 텍스트 입력 화면
- `/result` : 칼로리 결과 화면
- `/today` : 오늘 기록 화면
- `/mypage` : 마이페이지
- `/admin` : 관리자 화면

## 기본 흐름

1. 사용자가 `/`에서 앱을 시작한다.
2. `/login`에서 Google 로그인 또는 계속하기를 진행한다.
3. `/record`에서 사진 입력 또는 텍스트 입력을 선택한다.
4. 사진 입력이면 `/record/photo`로 이동한다.
5. 텍스트 입력이면 `/record/text`로 이동한다.
6. 입력값을 검증한 뒤 칼로리를 추정한다.
7. `/result`에서 추정 결과를 확인한다.
8. 사용자가 내용을 수정하거나 저장한다.
9. 저장 후 `/today` 또는 `/mypage`에서 기록을 확인한다.
10. `/admin`은 관리자만 접근한다.

## 처리 규칙

- 사진 입력과 텍스트 입력은 둘 다 지원한다.
- 결과는 항상 추정치로 보여준다.
- 사용자가 수정한 값이 있으면 최종값으로 반영한다.
- 저장 전에는 필수 입력값 검증을 수행한다.
- 관리자 화면은 일반 사용자에게 노출하지 않는다.
